import React from 'react';
import Navigator from './Navigator';

function Main(props) {
  /*return (
    <section style={ style }>
      <Navigator />
      { props.children }
    </section>
  );*/
  return <div>bla</div>;
}

const style = {
  minHeight: 'calc(100vh - 11em)'
}

export default Main;
